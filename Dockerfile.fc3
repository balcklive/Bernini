# =============================================================================
# Dockerfile.fc3 — Bernini for Alibaba Cloud FC3 (ultra-slim)
# =============================================================================
# Minimal image: CUDA runtime + PyTorch + essential deps only.
# flash-attn and optional heavy packages removed.
# =============================================================================

FROM nvidia/cuda:12.6.0-runtime-ubuntu22.04

ENV DEBIAN_FRONTEND=noninteractive
ENV TZ=Asia/Shanghai

LABEL org.opencontainers.image.title="Bernini (FC3)"
LABEL org.opencontainers.image.description="Bernini — slim build for Alibaba Cloud FC3"

# ── Minimal system deps ─────────────────────────────────────────────────────
RUN apt-get update && apt-get install -y --no-install-recommends \
    software-properties-common \
    && add-apt-repository -y ppa:deadsnakes/ppa \
    && apt-get update && apt-get install -y --no-install-recommends \
    python3.11 python3.11-venv \
    git \
    ffmpeg \
    && rm -rf /var/lib/apt/lists/*

RUN ln -sf /usr/bin/python3.11 /usr/bin/python && \
    ln -sf /usr/bin/python3.11 /usr/bin/python3

RUN python3 -m ensurepip --upgrade && \
    pip3 install --no-cache-dir --upgrade pip setuptools wheel

# ── Python dependencies ─────────────────────────────────────────────────────
WORKDIR /app

# PyTorch + CUDA (the heavy one)
RUN pip3 install --no-cache-dir \
    torch==2.7.1+cu126 \
    --extra-index-url https://download.pytorch.org/whl/cu126

# Core inference deps (no flash-attn, no decord, no av — saves space)
RUN pip3 install --no-cache-dir \
    diffusers==0.35.2 \
    accelerate==0.34.2 \
    transformers==4.57.3 \
    safetensors einops numpy Pillow tqdm \
    imageio imageio-ffmpeg \
    --extra-index-url https://download.pytorch.org/whl/cu126

# veomni
RUN pip3 install --no-cache-dir --no-deps \
    git+https://github.com/ByteDance-Seed/VeOmni.git@v0.1.11

# ── Source code ─────────────────────────────────────────────────────────────
COPY . .
RUN pip3 install --no-build-isolation -e . --no-deps

# ── Cleanup (remove pip cache, avoid bloat) ─────────────────────────────────
RUN rm -rf /root/.cache/pip

EXPOSE 7860

CMD ["python", "gradio_demo.py", "--config", "/mnt/bernini"]
